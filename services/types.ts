export type UUID = string;

export interface CustomizationSettings {
  headerLayout: string;
  theme: string;
  primaryColor: string;
  secondaryColor: string;
  titleFont: string;
  textFont: string;
  buttonFont: string;
  footerLayout: string;
}
